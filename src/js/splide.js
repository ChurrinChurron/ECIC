var splide = new Splide( '#splideCategorias', {
    type   : 'loop',
    perPage: 3,
    perMove: 1,
  } );
  
  splide.mount();

  var splide = new Splide( '#splideCursos', {
    type   : 'loop',
    perPage: 3,
    perMove: 1,
  } );
  
  splide.mount();

  var splide = new Splide( '#splideConvenios', {
    type   : 'loop',
    perPage: 3,
    perMove: 1,
  } );
  
  splide.mount();

  var splide = new Splide( '#splideOpinion', {
    type   : 'loop',
    perPage: 3,
    perMove: 1,
  } );
  
  splide.mount();
